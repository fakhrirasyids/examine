<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Examine" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Examine" parent="Base.Theme.Examine">
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Theme.Examine.Blue" parent="Base.Theme.Examine">
        <item name="android:statusBarColor">@android:color/holo_blue_dark</item>
    </style>
</resources>